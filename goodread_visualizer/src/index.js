import React from 'react';
import ReactDOM from 'react-dom';
import MainDiv from './main.js';

// Elements will be added via other js files.
ReactDOM.render(<MainDiv />, document.getElementById('root'));